//field, currentPlayer, win, draw

export const selectField = ({ field }) => field

export const selectCurrentPlayer = ({ currentPlayer }) => currentPlayer

export const selectDraw = ({ draw }) => draw

export const selectWin = ({ win }) => win
